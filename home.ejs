<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <title>TODO List - Trang chủ</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
  </head>
  <body
    class="bg-gradient-to-br from-[#0f2027] via-[#2c5364] to-[#f7ff00] min-h-screen flex flex-col"
  >
    <!-- Banner chào mừng: full width -->
    <header class="w-full">
      <div
        class="w-full bg-gradient-to-r from-cyan-500 via-blue-400 to-yellow-300 py-8 shadow-2xl flex flex-col items-center justify-center animate-fade-in"
      >
        <div class="flex items-center gap-3 mb-2">
          <span class="ph ph-sparkle text-4xl text-white drop-shadow-lg"></span>
          <span
            class="text-3xl md:text-4xl font-extrabold text-white tracking-wide drop-shadow-lg"
            >Chào mừng đến với my group Website!</span
          >
        </div>
        <p class="text-center text-white/90 text-lg font-medium">
          Todo List - Quản lý công việc hiệu quả.
        </p>
      </div>
    </header>

    <!-- Main content: dọc xuống, các card cách nhau -->
    <main class="flex flex-col items-center w-full px-2 mt-8 space-y-8 flex-grow">
      <!-- Giới thiệu chức năng -->
      <section class="w-full max-w-xl">
        <div
          class="bg-white/20 backdrop-blur-xl border border-cyan-300/30 rounded-3xl shadow-2xl p-7 animate-fade-in"
        >
          <h2
            class="text-2xl font-semibold text-cyan-300 mb-3 flex items-center gap-2"
          >
            <span class="ph ph-users-four"></span>
            Giới thiệu My Groups
          <!-- </h2>
          <ul class="space-y-3 text-cyan-200">
            <li class="flex items-center gap-2">
              <span class="ph ph-plus-square text-cyan-300"></span>
              <span
                >Tạo hoặc tham gia nhóm để chia sẻ và quản lý công việc.</span
              >
            </li>
            <li class="flex items-center gap-2">
              <span class="ph ph-list-checks text-cyan-300"></span>
              <span
                >Thêm, xóa, đánh dấu hoàn thành các công việc cá nhân hoặc
                nhóm.</span
              >
            </li>
            <li class="flex items-center gap-2">
              <span class="ph ph-cloud-arrow-up text-cyan-300"></span>
              <span>Đồng bộ hóa real-time giữa các thành viên.</span>
            </li>
          </ul> -->
        </div>
      </section>

      <!-- Thành viên nhóm: flex box theo cột -->
      <section class="w-full max-w-xl">
  <div
    class="bg-white/20 backdrop-blur-xl border border-cyan-300/30 rounded-3xl shadow-2xl p-7 animate-fade-in"
  >
    <h3
      class="text-xl font-semibold text-cyan-300 mb-4 flex items-center gap-2"
    >
      <span class="ph ph-user-list"></span>
      Thành viên nhóm
    </h3>
    <div class="flex flex-col gap-4">
      <% members.forEach(member => { %>
        <div
          class="flex items-center gap-4 bg-white/10 border border-cyan-400/30 rounded-xl p-3 shadow transition hover:scale-105"
        >
          <img
            src="<%= member.avatar %>"
            alt="<%= member.name %>"
            class="w-10 h-10 rounded-full border-2 border-cyan-300 shadow"
          />
          <div>
            <div class="text-cyan-200 font-semibold"><%= member.name %></div>
            <div class="text-cyan-100 text-sm">MSSV: <%= member.mssv %></div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</section>

      <!-- Khung TODO List & nút Start -->
      <section class="w-full max-w-xl">
        <div
          class="bg-white/20 backdrop-blur-xl border border-cyan-300/30 rounded-3xl shadow-2xl p-7 flex flex-col items-center animate-fade-in"
        >
          <div class="flex items-center justify-center gap-2 mb-2">
            <span
              class="ph ph-check-square text-2xl text-cyan-400 animate-bounce"
            ></span>
            <h1 class="text-2xl font-extrabold text-cyan-300 drop-shadow-lg">
              TODO List
            </h1>
          </div>
          <p class="text-cyan-200 font-medium mb-4 text-center">
            Quản lý công việc cá nhân & nhóm cực dễ dàng.
          </p>
          <button
            onclick="window.location.href='/todo'"
            class="group px-8 py-3 rounded-xl bg-gradient-to-r from-cyan-500 to-cyan-300 shadow-xl hover:scale-105 transition-transform duration-300 font-bold text-white text-xl relative overflow-hidden"
          >
            <span
              class="absolute inset-0 opacity-20 group-hover:opacity-40 transition bg-gradient-to-r from-cyan-400 via-cyan-300 to-blue-400 animate-pulse"
            ></span>
            <span class="relative z-10 flex items-center gap-2">
              <span
                class="ph ph-arrow-right text-white animate-move-right"
              ></span>
              Start
            </span>
          </button>
        </div>
      </section>
    </main>

    <style>
      .animate-fade-in {
        animation: fade-in 0.8s cubic-bezier(0.23, 1, 0.32, 1) forwards;
      }
      @keyframes fade-in {
        from {
          opacity: 0;
          transform: scale(0.98) translateY(30px);
        }
        to {
          opacity: 1;
          transform: scale(1) translateY(0);
        }
      }
      .animate-move-right {
        animation: move-right 1.2s infinite alternate;
      }
      @keyframes move-right {
        from {
          transform: translateX(0);
        }
        to {
          transform: translateX(8px);
        }
      }
    </style>
  </body>
  <!-- Footer -->
  <footer class="w-full bg-gradient-to-r from-cyan-500 via-blue-400 to-yellow-300 py-1 mt-12 shadow-inner">
    <div class="max-w-4xl mx-auto text-center text-white/90">
      <p class="font-medium">&copy; <%= new Date().getFullYear() %> My Group Website. </p>
      <div class="flex justify-center gap-4 mt-2">
        <a href="/" class="hover:text-white transition">Trang chủ</a>
        <a href="/todo" class="hover:text-white transition">TODO List</a>
        <a href="https://group-web-tailwind-1.onrender.com/" class="hover:text-white transition">Liên hệ</a>
      </div>
    </div>
  </footer>
</html>
